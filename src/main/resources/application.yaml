spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: postgres
    password: admin

    hikari:
      auto-commit: false
  flyway:
    baseline-on-migrate: true
